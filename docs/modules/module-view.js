function nav(){return `<header class="topbar"><div class="brand"><div class="sigil">ʸx</div><div>原力战略三部曲 · Modules</div></div><nav class="nav"><a href="../index.html">Home</a><a href="../trilogy-concept-map.html">Canon Map</a><a href="index.html">Modules</a><a href="../asset.html">Asset Tool</a><a href="../startup.html">Startup Tool</a><a href="../os.html">OS Tool</a><a href="https://github.com/moonstachain/yuanli-strategy-soul">GitHub</a></nav></header>`}
function list(items){return `<div class="list">${items.map(x=>`<div class="item"><b>${x.split('：')[0]}</b><span>${x.includes('：')?x.substring(x.indexOf('：')+1):x}</span></div>`).join('')}</div>`}
function section(title,body){return `<section class="section panel"><div class="pad"><div class="tag">${title}</div>${body}</div></section>`}
function renderIndex(){document.title='Modules · 原力战略三部曲十二模块';document.body.innerHTML=`<main class="shell">${nav()}<section class="hero"><div class="hero-card"><div class="pad"><div class="eyebrow">L5 · Module Index</div><h1>十二核心模块<br/><span>五六七级正典入口</span></h1><p class="lead">这里是原力战略三部曲的模块子页总入口。每个模块页面都按 L5-L7 结构展开：独立子页、模块内子结构、可执行任务与回写。</p><div class="formula" style="margin-top:18px">L5 模块独立子页\nL6 定义 / 边界 / 原子 / 输入 / 判断 / 输出 / 回写\nL7 模板 / 验收 / Codex-ready task / OS回写</div></div></div><aside class="hero-card"><div class="pad"><p class="quote">每个模块不是解释一个概念，而是解释一个可执行、可验收、可回写的系统组件。</p></div></aside></section><section class="section"><div class="section-head"><div><h2>模块索引</h2><p class="sub">点击进入每个核心模块的独立子页。</p></div></div><div class="grid3">${window.YUANLI_MODULES.map(m=>`<a class="card" style="text-decoration:none;color:inherit" href="${m.slug}"><div class="tag">${m.id} · ${m.label}</div><h3>${m.name}</h3><p>${m.subtitle}</p><p class="mono" style="margin-top:12px">${m.part}</p></a>`).join('')}</div></section><footer class="footer">Modules · 原力战略三部曲</footer></main>`}
function renderModule(m){document.title=`${m.id} · ${m.name} · 原力战略三部曲模块子页`;document.body.innerHTML=`<main class="shell">${nav()}<section class="hero"><div class="hero-card"><div class="pad"><div class="eyebrow">L5 Module Page · ${m.id} · ${m.label}</div><h1>${m.name}<br/><span>${m.subtitle}</span></h1><p class="lead">${m.definition}</p><div class="formula" style="margin-top:18px">所属：${m.part}\n工具接口：${m.tool}\n第一性问题：${m.question}</div><div class="cta-row"><a class="cta" href="index.html">返回模块索引</a><a class="ghost" href="../trilogy-concept-map.html">返回正典地图</a></div></div></div><aside class="hero-card"><div class="pad"><p class="quote">${m.firstPrinciple}</p></div></aside></section>${section('01 · 定义',`<h2>它是什么</h2><p class="lead">${m.definition}</p>`)}${section('02 · 不是它',`<h2>边界与误区</h2><div class="formula">${m.not}</div>`)}${section('03 · 第一性问题',`<h2>${m.question}</h2><p class="sub">这个问题是本模块不可替代的存在理由。回答不了这个问题，模块就只是概念装饰。</p>`)}${section('04 · 原子模块',`<h2>内部组件</h2>${list(m.atoms)}`)}${section('05 · 输入材料',`<h2>进入本模块之前必须准备什么</h2>${list(m.inputs)}`)}${section('06 · 判断标准',`<h2>怎么判断它是否成立</h2>${list(m.standards)}`)}${section('07 · 输出物',`<h2>本模块必须产出什么</h2>${list(m.outputs)}`)}${section('08 · 回写位置',`<h2>输出必须写回哪里</h2>${list(m.writeback)}`)}${section('09 · 可执行任务',`<h2>L7 · Codex-ready / 人机协作任务</h2>${list(m.tasks)}`)}${section('10 · 常见陷阱',`<h2>反模式清单</h2>${list(m.traps)}`)}${section('11 · 下一步接口',`<h2>递进关系</h2><div class="formula">${m.next}</div>`)}<section class="section panel"><div class="pad"><h2>最终验收</h2><div class="formula">这个模块是否有明确输入？\n是否有清晰判断标准？\n是否生成了可复用输出？\n是否写回到文脉、大脑、地图、链路或母体？\n是否让下一次更强？</div></div></section><footer class="footer">${m.id} · ${m.name} · 原力战略三部曲模块子页</footer></main>`}
const id=document.body.dataset.module;if(id){renderModule(window.YUANLI_BY_ID[id])}else{renderIndex()}