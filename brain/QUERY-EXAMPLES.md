# Yuanli Brain · QUERY EXAMPLES

Date: 2026-06-30
Phase: C2 Markdown Brain Phase 2

## Purpose

定义原力大脑的第一批可测试查询，用来验证 C2 是否能从“文件索引”升级为“战略判断系统”。

## Standard Answer Format

```text
1. 直接结论
2. 证据来源
3. 关联模块
4. 当前成熟度
5. 未知缺口
6. 下一步 C4 行动
7. 回写建议
```

## Query 1 · B2 是否可以产品化？

```text
B2 品类独创现在是否可以产品化？
```

Expected:

```text
结论：可以做小范围验证，不建议直接规模化售卖。
证据：B2 seed asset package 已完成；maturity 27/40；P0。
缺口：真实验证仍弱，需 naming-test.md 与 user-language.md。
下一步：创建 B2 Deepening Task。
回写：试跑结果写入 validation-records.md，并更新 scorecard。
```

## Query 2 · 哪些模块最该先验证？

```text
从成熟度评分看，哪些模块最该先做真实验证？
```

Expected:

```text
P0：A4、B1、B2、B3、B4、C4。
P0.5：C2。
理由：这些模块决定市场验证、商业化和 OS 行动闭环。
```

## Query 3 · C2 现在缺什么？

```text
C2 一个大脑目前距离产品化还缺什么？
```

Expected:

```text
C2 已达到 Strategy+，Total 31/40，但 Validation 仍为 1/5。
缺口：retrieval-test、tagging-template、writeback-examples、validation-records、真实查询试跑。
```

## Query 4 · 某个新案例应该写回哪里？

```text
我有一个学员通过 A1 找到母体的真实案例，应该写回哪里？
```

Expected:

```text
写回 assets/A1-发现母体/real-learner-cases.md 或 brain/cases/。
如果能验证 A1 作业有效，也写入 A1 evidence。
如果形成可复用教学规则，写入 evolution note。
```

## Query 5 · PMO 下一步是什么？

```text
当前 PMO 下一步最重要的任务是什么？
```

Expected:

```text
先创建 C2 Brain Validation 与 P0 Deepening 任务。
然后为 A4、B1-B4、C4 补齐验证类文件。
```

## Validation Rule

每个查询都要检查：

```text
是否给出结论？
是否引用已有文件？
是否说明缺口？
是否给下一步？
是否建议回写？
```
